# Current status:
Executable side

cargo verus verify
tocuh storage.bin
cargo verus build
RUST_BACKTRACE=1 ./target/debug/verisplinter

Currently broken at Implementation.rs:1392 : false placeholder

# Commands
verus -Zunpretty=expanded src/main.rs> /tmp/foo
time verus src/main.rs --verify-module implementation::Implementation_v
~/bin/verus-watcher.py -w 'src/**/*.rs' -w 'src/**/**/*.rs' verus src/main.rs --expand-errors --multiple-errors 5 --verify-module implementation::Implementation_v

------------------------------------------------------------------------------

Where I want to go next: I want to complete the test execution specified
in trusted/ClientAPI_t.rs new(), which should write some stuff, sync it,
crash, and then read back what it wrote.

`cargo verus build` needs to work again.


------------------------------------------------------------------------------

~/bin/verus-watcher.py -w "*/*.rs" -w "*.rs"  verus sequential_main.rs --compile

Verification side

- Placeholder data structures -- get plumbing in place
    - empty store + journal-fits-in-superblock.
        - Put only, no get
        - forces marshalling with journal
    - vecmap store + journal, everything fits in superblock.
        - services Gets

- Real data structures
    - pry journal out of superblock and use block indirection instead
        - journal structure follows allocationjournal (implement the real thing)
        - exercises coordination system protocol
        - need to begin implementing allocation
    - replace vecmap with betree
        - maybe b+tree is still just inline vecmaps?
            (bring down allocationbetree, but not b+tree)

------------------------------------------------------------------------------

What's missing?
Sync:
- send_superblock is all missing
- handle_disk_superblock_write_response is broken in the middle
- the journal never marshals the unmarshalled tail, which will involve the cache
- preparing to send a superblock will require learning the journal pages are
    all clean.

Recovery:
- handle_disk_cache_response unwritten, because there's no cache
- no journal index recovery procedure
- no map journal replay recovery procedure

handle_query works because the map is all in memory.
handle_put works because the journal can be appended without the cache

------------------------------------------------------------------------------

Things Jon needs

🦆 JL: CachedJournal needs a view up to something with a MsgHistory, surely?
🦆 CH: Can I return slice refs?

